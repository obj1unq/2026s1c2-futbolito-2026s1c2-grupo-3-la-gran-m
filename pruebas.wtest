import objetos.*
import pgmProgram.*
describe "lionel pateando" {
  test "pateando la pelota" {
    lionel.position(game.at(5,5))
    lionel.patear()
    assert.equals(game.at(8,5), pelota.position())
  }
}